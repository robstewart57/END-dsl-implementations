use END: all ;
use Array: all ;
use StdIO: all;
use String: {string};

// Use a Makefile for these programs, using:
// SAC2C = sac2c -t mt_pth

int main()
{
  img = imread("../images/maisie.png");
  img, start = timestamp( img);
  img, end = timestamp( img);
  printf("%f\n", difftime( end, start));
  /* imwrite("out.png", img); */
  return 0;
}
